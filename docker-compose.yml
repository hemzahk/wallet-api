version: "3.8"

services:
  db:
    image: postgres:16.3
    container_name: wallet-db
    environment:
      POSTGRES_DB: wallet
      POSTGRES_USER: LKGVqjLa
      POSTGRES_PASSWORD: QEtxBn5NKnqQ4NvGKsWu
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  db-data:
